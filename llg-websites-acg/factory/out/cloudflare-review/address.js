// Google Places autocomplete disabled — the address field now behaves as a normal text input.
// To re-enable, uncomment the block below and restore the Google Maps script in the HTML pages.
/*
const initAutocomplete = () => {
  // Create the autocomplete object, restricting the search to geographical location types.
  const autocomplete = new google.maps.places.Autocomplete(
    document.querySelector("[data-address]"),
    { types: ["geocode"] }
  )
}

// Call this function when the page is loaded
google.maps.event.addDomListener(window, "load", initAutocomplete)
*/
